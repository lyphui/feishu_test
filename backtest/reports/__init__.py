# 报告层：一切出图（report / batch_report / bull_report / plotting /
# intraday_report / lu_macd_report）。
#
# 分层判据（CLAUDE.md）：**import matplotlib 的进 `reports/`**；
# 计算与数据工具在 `lib/`；具体策略在 `strategies/`；CLI 编排在 `scripts/`。
