[project]
name = "feishu-test"
version = "0.1.0"
description = "飞书股市分析 + MACD 量化回测平台"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["strategies*", "utils*", "config*"]
